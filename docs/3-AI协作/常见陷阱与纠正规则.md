# 常见陷阱与纠正规则

⚠️ 协作版本 v1.0
最后更新：2026-06-11
维护人：[廖雨辰]

---

## 变更记录

| 版本 | 日期 | 变更内容 | 变更人 |
|------|------|----------|--------|
| v1.0 | 2026-06-11 | 初始创建（10 条常见陷阱+纠正规则） | [廖雨辰] |

---

## 陷阱索引

| 编号 | 陷阱名称 | 严重程度 |
|------|----------|----------|
| T01 | ResultSet 未关闭 | 🔴 致命（内存泄漏） |
| T02 | List 中使用 ForEach 全量渲染 | 🔴 致命（性能崩坏） |
| T03 | 异步操作缺少 await | 🔴 致命（逻辑错误） |
| T04 | 网络请求未处理异常 | 🟡 严重（用户体验差） |
| T05 | 硬编码颜色/尺寸 | 🟡 严重（维护困难） |
| T06 | Token 存储方式错误 | 🟡 严重（登录态丢失） |
| T07 | 路由传参未类型化 | 🔵 建议（类型安全） |
| T08 | @Observed 类未正确实现 | 🟡 严重（UI 不更新） |
| T09 | 自定义组件未封装 | 🔵 建议（代码冗余） |
| T10 | 图表使用硬编码数据 | 🔴 致命（不满足硬指标） |

---

## 纠正规则详情

### 🔴 T01：ResultSet 未关闭

**描述**：查询数据库后忘记调用 `resultSet.close()`。

**错误示例**：
```typescript
const resultSet = await this.rdbStore.query(predicates);
while (resultSet.goToNextRow()) {
  // 读取数据
}
// 缺少 close() ← 这里会内存泄漏
return dataList;
```

**正确示例**：
```typescript
let resultSet: relationalStore.ResultSet | null = null;
try {
  resultSet = await this.rdbStore.query(predicates);
  while (resultSet.goToNextRow()) {
    // 读取数据
  }
} finally {
  resultSet?.close();  // 确保关闭
}
```

**自查提醒**：生成任何 RDB 查询代码时，AI 必须输出：`[自查提醒] 请确认 ResultSet 已放入 try-finally 中关闭`。

---

### 🔴 T02：List 中使用 ForEach 全量渲染

**描述**：长列表直接用 `ForEach` 渲染所有数据项，导致性能问题。

**错误示例**：
```typescript
Column() {
  ForEach(this.poemList, (item) => {
    Text(item.title)
  })
}
```

**正确示例**：
```typescript
List() {
  LazyForEach(this.poemListDataSource, (item) => {
    ListItem() {
      PoemListItem({ poem: item })
    }
  })
}
```

**自查提醒**：生成列表相关代码时，AI 必须检查：如果数据量可能超过 20 条，强制使用 `List + LazyForEach`。

---

### 🔴 T03：异步操作缺少 await

**描述**：调用异步函数时忘记使用 `await`，导致时序错误。

**错误示例**：
```typescript
this.db.insertPoem(poem);  // 缺少 await
this.loadData();           // 可能在 insert 完成前执行
```

**正确示例**：
```typescript
await this.db.insertPoem(poem);
await this.loadData();
```

**自查提醒**：所有 `relationalStore` 和 `axios` 操作前必须加 `await`，并包裹在 `try-catch` 中。

---

### 🟡 T06：Token 存储方式错误

**描述**：使用普通变量或 `AppStorage` 而不持久化，导致应用重启后登录态丢失。

**错误示例**：
```typescript
let token = '';  // 应用重启后丢失
AppStorage.setOrCreate('token', token);  // 未加 persist
```

**正确示例**：
```typescript
PersistentStorage.persistProp('token', '');
const token = AppStorage.get('token');
```

**自查提醒**：涉及登录态存储的代码，检查是否使用了 `PersistentStorage.persistProp`。

---

### 🔴 T10：图表使用硬编码数据

**描述**：数据可视化图表使用静态假数据，而不是从 RDB 实时聚合查询。

**错误示例**：
```typescript
@State chartData = [
  { dynasty: '唐', count: 120 },
  { dynasty: '宋', count: 100 }
]  // 硬编码，不是从 RDB 查的
```

**正确示例**：
```typescript
aboutToAppear() {
  this.loadChartData();  // 从 RDB GROUP BY dynasty 查询
}
async loadChartData() {
  const sql = 'SELECT dynasty, COUNT(*) as cnt FROM poem GROUP BY dynasty';
  // 执行查询，赋值给 @State 变量
}
```

**自查提醒**：生成图表相关代码时，AI 必须确认数据来源于 RDB 查询，并输出数据来源说明。

---

## 附录：AI 输出格式要求

每次 AI 生成的代码块末尾必须包含一个 **自查清单**：

```markdown
[自查清单]
- [ ] ResultSet 已在 finally 中关闭
- [ ] 列表使用 LazyForEach 而非 ForEach
- [ ] 所有异步操作已添加 await 和 try-catch
- [ ] Token 使用 PersistentStorage 持久化
- [ ] 图表数据来源于 RDB 聚合查询
```